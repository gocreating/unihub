
---
description: List all Git worktree statuses
---

List the status of all current Git worktrees.

Run `git worktree list` and format the output:

- Show worktree path
- Show associated branch
- Mark the current worktree with \*
