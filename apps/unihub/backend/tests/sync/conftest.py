"""Shared fixtures for sync tests.

The ``bare_repo`` fixture moved to ``tests/conftest.py`` (016 round 2) so the
EntityView data_io/sync round-trip suite can use it too; sync tests resolve it
from the parent conftest unchanged.
"""
