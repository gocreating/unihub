# Claude Development Guidelines — unihub Monorepo

**unihub** is a single, growing dashboard that serves as a personal central hub — one place to manage and visualize all dimensions of a user's daily life. As the project evolves, new domains get connected to the hub: finance, visiting, language, people, music, and whatever comes next. The dashboard frontend is the consistent interaction surface; backends and data sources expand behind it over time.

> **Reference implementation**: `ov-fleet` (`/Users/gocreating/projects/OverviewCorporation/ov-pro-tools/apps/ov-fleet`) — the primary architectural reference. Follow its patterns for backend layout, service layer, and frontend organization.

## Core Architecture: Entity-Centric Domains

Every domain is built around **entities** — the user creates and manages entities scoped to that domain. There is one backend (one Django project, one database) with each domain implemented as a standalone Django app inside it.

```
apps/
  unihub/
    frontend/    # Single hub SPA
    backend/     # Single Django project — one DB, domain apps inside
      unihub/    # Django project (settings, urls, wsgi)
      core/      # Shared infrastructure (filters, pagination, permissions)
      finance/   # Django app — finance entities
      visiting/  # Django app — visiting entities
      language/  # Django app — language learning (WordCard, GrammarSheet)
      people/    # Django app — people & relationships (Person, Relationship)
      music/     # Django app — song collection (Song)
      data_io/   # Data import/export
      sync/      # Data sync with external sources
      system/    # System settings (profile, etc.)
      health/    # Health check endpoint
    docker-compose.local.yml       # Build from source, local dev
    docker-compose.production.yml  # Pre-built images, production
    specs/
.env.example                       # Template for production secrets (copy to apps/unihub/.env)
```

Adding a new domain = adding a new Django app under `apps/unihub/backend/`, registering it in `INSTALLED_APPS` and `urls.py`, and adding a `pages/<domain>/` section in the frontend.

Per-app CLAUDE.md files:
- `apps/unihub/frontend/CLAUDE.md` — frontend dev guidelines
- `apps/unihub/backend/CLAUDE.md`  — backend dev guidelines

## Architecture Decisions

### Frontend

**Vite over @umijs/max**
- Rationale: Lighter build toolchain, faster HMR, standard React setup. ov-fleet uses UmiJS but that adds framework lock-in we avoid for a personal project.

**Ant Design 5 + Pro Components for UI**
- Rationale: Enterprise-grade admin framework matching ov-fleet's UI stack. Rich data display components (ProTable, StatisticCard, charts).

**PageTable as default tabular component**
- All tabular data views MUST use `PageTable` (`src/components/PageTable/`), adapted from ov-fleet. It provides sticky header, sticky footer, sticky horizontal scrollbar, and column-follow-on-scroll — pre-configured. Use the exported helpers `widthForHeader()`, `measureTextWidth()`, `computeScrollX()` for column widths.

**TanStack React Query for data fetching**
- Rationale: Matches ov-fleet's data-fetching layer. Excellent caching and loading/error states.

**pnpm as frontend package manager**
- Rationale: Strict dependency resolution, disk efficient, mature workspace support.

### Backend

**Django + Django REST Framework**
- Rationale: Mirrors ov-fleet's proven backend stack. Rich ORM, DRF serializers/viewsets, session auth, and drf-spectacular for OpenAPI generation.

**PostgreSQL**
- Rationale: Same as ov-fleet. Reliable relational store; supports JSONB for flexible tag/metadata schemas.

**drf-spectacular for OpenAPI**
- Rationale: Auto-generates `openapi.yaml` consumed by the frontend's type generation (`openapi-typescript`). Types stay in sync with the API contract.

**App-level isolation**
- Rationale: Each app has its own `package.json` / `pyproject.toml` and dependencies. No shared root lockfile.

## Backend Structure (follow ov-fleet)

New Django apps under `apps/unihub/backend/` should mirror ov-fleet's layout:

```
backend/
  <project_name>/         # Django project root
    settings.py
    urls.py               # Root URL router — include app-level urls.py
    wsgi.py / asgi.py
    auth/                 # Session auth + RBAC (IsAdminOnly, etc.)
    <domain>/             # One Django app per domain (finance, visiting, language, …)
      models.py
      views.py            # DRF ViewSets
      serializers.py
      filters.py          # Query/filter helpers
      urls.py
      migrations/
    health/               # Health-check endpoint
  tests/                  # pytest-django test suite
  manage.py
  pyproject.toml          # uv-managed Python dependencies
  Dockerfile
  entrypoint.sh
```

**Key backend conventions (from ov-fleet):**
- Session-based authentication (Django's built-in + DRF session auth)
- Role-based permissions via DRF permission classes (`IsAdminOnly`, etc.)
- OpenAPI schema at `/api/docs/` via drf-spectacular Swagger UI
- Background tasks via django-q2 where polling or async work is needed
- All HTTP client calls via `httpx`
- Linter: `ruff`; tests: `pytest-django`

## Frontend Service Layer (follow ov-fleet)

Organise API calls under `src/services/<backend-name>/`:

```
src/services/unihub-backend/
  finance.ts        # Finance domain endpoints
  auth.ts           # Authentication endpoints
  core.ts           # Shared/core endpoints
  io.ts             # Data import/export endpoints
  sync.ts           # Data sync endpoints
  system.ts         # System (profile, settings) endpoints
  index.ts          # API_BASE_URL + service exports
```

Types are auto-generated from `openapi.yaml` via `openapi-typescript`. Do not hand-write API response types.

## Development Conventions

### TypeScript/React Style
- Strict TypeScript (`strict: true` in tsconfig)
- Functional components with hooks (no class components)
- Named exports preferred over default exports

### Tooling
- **Frontend package manager**: `pnpm` — never use npm or yarn directly
- **Backend package manager**: `uv` — never use pip directly
- **Frontend linter**: ESLint
- **Backend linter**: ruff
- **Testing (frontend)**: Vitest + React Testing Library
- **Testing (backend)**: pytest-django

### Quality Loop

Frontend — run from `apps/unihub/frontend/` after every change:

```bash
pnpm lint
pnpm typecheck
pnpm test
```

Backend — run from `apps/unihub/backend/` after every change:

```bash
uv run ruff check .
uv run pytest
```

### Git
- Branch from `main`
- Commit messages: imperative mood, concise

## Adding a New Domain

When connecting a new dimension to the hub:
1. Create `apps/unihub/backend/<domain>/` as a new Django app (`models.py`, `views.py`, `serializers.py`, `urls.py`, `migrations/`)
2. Register the app in `INSTALLED_APPS` and add its URL prefix in `unihub/urls.py`
3. Seed the domain's system AttributeDefinitions via a data migration or management command — never hardcoded in application code
4. Add the domain's pages under `apps/unihub/frontend/src/pages/<domain>/`
5. Add a nav section entry in `AppShell.tsx` using a `menu.*` i18n key (constitution Principle VIII)
6. Add a service file at `apps/unihub/frontend/src/services/<domain>.ts` with types generated from the updated OpenAPI schema

<!-- SPECKIT START -->
## Active Feature

**Branch**: `016-entity-views` | **Plan**: [specs/016-entity-views/plan.md](specs/016-entity-views/plan.md)

Entity Views from GitHub issue #19: save pre-configured filtering + sorting + column visibility/ordering + page size as named per-user, per-table **views**. **Iteration 1 (2026-07-21) shipped** — backend `EntityView` model (nanoid PK, owner FK, unique (owner, table_key, name), migration core/0005) + owner-scoped ViewSet at `/api/v1/core/entity-views/` (plain-array list, `?table_key=`, position-append on create, bulk `reorder/` action; 11-test TDD suite; data_io registration EXPLICITLY DEFERRED — the registry's natural-key path is contenttypes-only and cannot represent the auth.User owner FK, recorded in core/apps.py). Frontend `components/EntityViews/`: `serialization.ts` (`view[<tableKey>]` inner mini-format per the issue's samples — type/id/filters/ordering/columns/pin/page_size/page; parse never throws → Tabular fallback + warning; `normalizeConfig` dirty compare), `useViewTabsState` (sessionStorage `unihub.views.<tableKey>` — tabs survive reloads within the session only), `useEntityViews` (virtual undeletable Tabular tab `__default__`, pinned-view merge in position order, open/switch/close/duplicate `X (n)` first-unused-suffix, save vs save-as ('needs-name' → SaveViewModal), FR-019 delete-converts-open-tab-to-anonymous, FR-021 column-drift reconciliation on BOTH baseline and snapshot, URL inbound/outbound effects — NOTE: outbound must never reset the inbound processed-value guard (`lastProcessedRef` separate from `lastParamRef`), else the two effects ping-pong forever on a malformed param), `ViewTabs` (`[+] tabs… [View ▾]` via new PageTable `viewBar` slot; strip `overflow-x: auto`, fixed edges; dirty dot; OverflowTooltip labels), `ViewDropdown` (open list, Save enabled-on-dirty, Duplicate, Edit…), staged `ManageViewsModal` (rename/pin/SortableList-reorder/delete — ZERO API before Save; deletions gate on Modal.confirm okType danger with ICU-plural count). Hooks gained view-level loaders: `loadGroups`/`loadRules` (bumps panelApplyCount)/`loadState`; `useEntityTable` gained `tableKey`/`snapshotConfig`/`loadConfig(config, {offset})` (offset survives the filter-change page reset via a skip ref). Adopted on all 5 entity list pages (tableKeys: `finance-currencies`/`finance-accounts`/`finance-exchange-rates`/`inventory-catalog`/`inventory-scenarios`); PageTable remount keys append the active tab id. e2e `entity-views.spec.ts` (row geometry at narrow viewport + deep-links). Test gotcha: AntD icon aria-labels join accessible names — `/^view/i` not `/view/i` (collides with "New view tab"). **Round 2 SHIPPED (2026-07-23)** — eight clarified directives (backend 477 pytest / frontend 695 vitest green — 2 pre-existing full-suite-load flakes in balance-sheets/SyncTab pass in isolation; e2e `entity-views.spec.ts` updated, awaits a human run against the dev stack): (1) **Tabular→"Table"** rename (i18n key `common.entityViews.tabular`→`defaultTable`, zh-TW 「表格」). (2) **default view is a PLAIN view** — page-provided initial name (`useEntityViews`/`useEntityTable` gained `defaultViewName`; catalog passes `'YTD'`); first save OR rename of the virtual default POSTs `is_default:true, pinned:true` materializing an `EntityView` row, thereafter its stored config is the tab's baseline; `is_default` BooleanField + partial `UniqueConstraint(owner, table_key, WHERE is_default)` (migration core/0006), serializer create-only (PATCH change→400), viewset DELETE→400; the default tab is ALWAYS first, excluded from manage-modal drag + delete (rename/pin stay), unpinnable (fallback guaranteed by the flag not the pin). (3) **"+" right of the last tab, always visible** — `ViewTabs` reordered to `[strip flex:0 1 auto][+ flex:0 0 auto][spacer][View]`; "+" lives OUTSIDE the scrolling strip so overflow scrolls the tabs while "+" stays docked at the strip's right edge. (4) **double-click tab = inline rename** — Enter/blur commit, Esc cancel, 400 collision keeps the input open + `message.error`; anonymous tab dbl-click opens `SaveViewModal` (name-and-save); `renameTab` on the virtual default materializes it. (5) **readable URL grammar** — REWROTE `serialization.ts`: per-facet `<tableKey>.view=<NAME>` (saved ref BY NAME, incl. the page default name while virtual) / repeatable `.f=or(attr op val; …)` / `.sort=<rulesToOrdering>` / `.cols=key~left,key,key~right` / `.size` / `.page`; minimal-encoding emitter (`buildSearchString` keeps `( ) ; : , ~` literal, spaces→`%20`), `URLSearchParams` parser; clean default tab emits NO params; legacy packed `view[<tableKey>]` format DROPPED (unparsed → default fallback); `useEntityViews` outbound now uses `navigate()` on `[...foreignParams, ...viewEntries]` — the inbound guard tracks a JSON of the view entries (NOT the raw string), `lastProcessedRef`/`lastParamRef` separation preserved. (6) **ViewConfig v2 per-column pins** — `ViewColumn.pin?: 'left'|'right'` replaces the view-wide `stickyLeft`/`stickyRight` booleans (removed everywhere: types, `snapshotConfig`/`loadConfig` map 1:1 to `ColumnDef.pin`, `useColumnConfig.loadState` dropped its `sticky` arg, all 5 pages' `defaultViewConfig`); migration 0006 data-migrates stored configs (`migrate_config`: stickyLeft→`pin:'left'` on first visible by order, stickyRight→last visible) and `upgradeConfigShape` upgrades v1 shapes read from sessionStorage; closes the round-1 boolean-pair projection gap (memory [[project_016_pin_projection]]). (7) **data_io deferral RESOLVED** — new `TableDescriptor.owner_field` (`core.entityview` registered with `owner_field="owner"`, `auto_system_fields(exclude={"owner_id"})`): the owner column is NEVER exported/diffed and is stamped with the acting user on import; `apply_diff`/`apply_selected`/`import_from_clone` gained an `acting_user` param threaded from `request.user` at `ImportConfirmView`/`ImportZipConfirmView`/batch-confirm/`SyncCheckoutConfirmView` (FR-024, SC-008; owner PKs never serialized → no phantom diffs). (8) **view row AUTO-HIDES** when only the default view/tab exists — `ViewTabs` renders a compact `TableOutlined` reveal affordance (Badge dot when the hidden default is dirty) in the SAME `viewBar` slot; `collapsed` derived in `useEntityViews`, `revealed` flag persisted in `unihub.views.<tableKey>` sessionStorage; the collapse decision uses the URL-view-state captured ONCE at mount (`initialUrlHadViewStateRef`) — NOT live params, because the lone materialized default round-trips its own config through the URL and would otherwise read as URL-addressed state. NOTE: `bare_repo` fixture moved from `tests/sync/conftest.py` to `tests/conftest.py` (shared with `test_entity_views_io.py`). Backend migration 0006 must be applied on next deploy (running docker stack unmigrated per [[project_prod_stack_and_sync_incident]]). Research R14–R21. **Round 3 SHIPPED (2026-08-03)** — seven interaction directives + one backend contract change (backend 487 pytest / frontend 725 vitest green; research R22–R28): (1) **hidden scrollbar + edge shadows** — the strip keeps `overflow-x:auto` with `scrollbarWidth:'none'`/`::-webkit-scrollbar{display:none}` and absolutely-positioned `view-tabs-shadow-left|right` gradient overlays derived from `scrollLeft`/`clientWidth`/`scrollWidth` (exported pure `overflowSides()` — unit-testable; live updates via `onScroll` + `ResizeObserver`). (2) **drag-to-reorder** — shared `SortableList` gained an additive `orientation?: 'vertical'|'horizontal'` prop (horizontal wrappers get `flex:none;display:flex`); `reorderTabs(orderedTabIds)` composes the table's COMPLETE id order (strip saved views, then non-open views in current relative order) and POSTs the existing `reorder/` — a partial list would strand other views at stale positions. (3) **per-tab menu** `ViewTabMenu` — CONTROLLED AntD Dropdown (`trigger={[]}`, open state owned by ViewTabs and keyed by tabId): left-click opens it only when the tab is ALREADY active, otherwise `switchTab`; right-click opens it on any tab with `preventDefault()`; items Save/Close/Duplicate/Pin-Unpin/Set as default/Rename/Delete, inapplicable ones DISABLED not hidden (matrix in data-model §7), Delete behind `Modal.confirm okType danger`. GOTCHA: dnd-kit's `attributes` include `role="button"` — spread `handleProps` BEFORE `role="tab"` or the tab loses its accessible role/name. (4) per-tab `×` and the **double-click rename gesture REMOVED** (Rename is a menu item running the same inline-input flow; anonymous tabs open `SaveViewModal`). (5) **`ViewKebab` replaces both** the "+" and the "View ▾" control at the row's right edge (Add empty view / Open ▸ listing only views not already open with a disabled empty state / Manage views…); `ViewDropdown.tsx` DELETED. (6) **`is_default` is a TRANSFERABLE role** — `validate_is_default` now rejects only an explicit `false` on the holder; `EntityViewSerializer.update()` clears the incumbent FIRST inside `transaction.atomic()` (the partial unique constraint is per-statement, NOT deferrable) then saves with `pinned=True`; the demoted row keeps pin/position/name/config; DELETE guard follows the role; NO migration (0006 already has the column) and the OpenAPI schema is byte-identical (validation-only change) — `EntityViewPatch` gained `is_default`. Frontend `setDefaultTab` SWAPS tab identities in place (the `kind:'default'` tab always represents the role holder): the promoted tab takes `DEFAULT_TAB_ID`, the demoted one becomes a plain saved tab — or an **anonymous** tab when the page default was still virtual (nothing to demote). (7) the round-2 **"default tab is ALWAYS first" invariant is DROPPED** — merge order is pure `position` (virtual default keeps the leading slot), the manage modal's default row is now IN the sortable list (rename enabled; pin + delete disabled, not hidden), and promotion never reorders. Also: hook actions are all TAB-ADDRESSED (`saveTab`/`saveTabAs`/`duplicateTab`/`pinTab`/`setDefaultTab`/`deleteTab`/`reorderTabs`; `saveActiveTab*`/`duplicateActiveTab` gone) because a right-click menu can target an INACTIVE tab; `ViewTabState` gained `isDefault`; FR-018 made every tab except the default holder closable, with a session `closedPinnedRef` so a closed pinned tab doesn't bounce back on the next refetch. e2e `entity-views.spec.ts` rewritten (7 tests: kebab docking at 375px, zero-scrollbar + three-position shadow probe, real-mouse drag with reload persistence) — NOT run here: the live dev stack serves the user's real data and the drag test writes saved views, so it awaits a human run like rounds 1–2. Migration 0006 still needs applying on the running docker stack ([[project_prod_stack_and_sync_incident]]). **Round 4 SHIPPED (2026-08-04)** — eight directives (backend 492 pytest / frontend 731 vitest green; research R29–R36): (1) **Save NEVER prompts** — `SaveViewModal` DELETED; `saveTab` on a tab with no `viewId` POSTs under the tab's current label and returns `'saved'` in every path (the `'needs-name'` outcome and `saveTabAs` are gone); a scratch tab is auto-labelled "New view" at creation. (2) **Rename is a prefilled dialog** — new `RenameViewModal` (auto-focused, trims, rejects blank, Enter submits, `maskClosable` only while untouched) replaces the round-3 inline input; on a tab with no stored view it relabels locally until Save. (3) **names are non-unique trimmed LABELS** — **migration core/0007** drops `UniqueConstraint(owner, table_key, name)`; the serializer's collision branch is gone (`validate_name` keeps strip + reject-blank); Duplicate keeps the source name verbatim (no `(n)` suffix). `TestNonUniqueNames` covers duplicate create/rename, trimming, blank rejection, and that duplicates don't confuse the default role. (4) **URL `.view` carries the ID** — `serializeSavedEntries(tableKey, viewId, …)`, `ParsedViewState.viewId`; renames no longer break deep links, and the round-2 "matches the page default name while virtual" special case is gone (a tab with no stored view serializes inline). SC-007 was amended to permit this ONE non-prose value. (5) **promotion disturbs nothing** — TWO defects fixed: the pinned-merge effect is now ORDER-PRESERVING (it keeps open tabs where they are and inserts only genuinely NEW pinned views, slotted by `position`) because promotion pins its target and the old rebuild-from-position made session tabs jump; and demoting a VIRTUAL page default now MATERIALIZES it as an ordinary view instead of converting it to an always-dirty scratch tab (that conversion was the reported "previous default becomes dirty" bug). (6) **dragged tabs stop stretching** — `SortableList` uses `CSS.Translate.toString()` for `orientation: 'horizontal'` (keeping `CSS.Transform` for vertical): `CSS.Transform` also emits the `scaleX` that `horizontalListSortingStrategy` computes from the passed item's width, which distorts variable-width tabs. (7) **"Manage views" removed** — `ManageViewsModal` + `commitManageChanges`/`ManageChanges` DELETED; every action is tab-addressed and a closed view is managed by opening it from the kebab's "Open ▸" first. The kebab now has exactly two entries. (8) **"Add empty view" is BLANK** — exported `blankConfig(defaults)` (no filters/sort, every column visible in declared order, no pins, page default size); `addAnonymousTab` → `addBlankTab`. Plus the tab menu now dismisses on outside click/Esc via a document listener (rc-trigger wires none because the Dropdown uses `trigger={[]}`). Locale keys added (`renameViewTitle`) and removed (`saveViewTitle`/`duplicateName`/`manageTitle`/`manageViews`/`manageSaveError`/`savedList`/`noSaved`/`edit`) in both locales. OpenAPI schema byte-identical (constraint change only). e2e `entity-views.spec.ts` gained the SC-010 mid-drag width lock (±2px) and a Rename-dialog-based `createSavedView` helper — 8 tests listed, NOT run here (the live stack serves real data). **Migrations 0006 AND 0007 are both pending on the running docker stack.** Test gotchas: AntD keeps a closed Dropdown MOUNTED (assert `.ant-dropdown-hidden`, not absence); dnd-kit's drag attributes set `role="button"`, so `handleProps` must be spread BEFORE `role="tab"`. **Round 5 SHIPPED (2026-08-04)** — three directives, frontend only (frontend 752 vitest green, backend 492 unchanged; research R37–R39): (1) **tabs are PER-VISIT** — `useViewTabsState` no longer restores or persists `tabs`/`activeTabId` (its sessionStorage payload is now just `{revealed}`, read tolerantly so a stale round-4 payload contributes only its flag); the mount rehydrate effect is gone and every page load rebuilds the row from the pinned-view merge plus the URL's view (FR-018/SC-013). NEW `useViewTabsState.test.ts` (7 tests) locks the store. BUG FOUND + FIXED while wiring this: the inbound URL effect created a DUPLICATE tab when the URL addressed a view the pinned merge had just opened — it read a stale `tabs` closure, so the existence check moved INSIDE the `setTabs` updater and `setActiveTabId` takes the updater form (React runs it after the setTabs updater, which is what assigns the id). (2) **shared `components/ConfirmDialog/`** — `confirmDialog({title, content, okText, cancelText, danger, onOk})` renders a plain AntD `Modal` with an explicit `space-between` footer (Cancel LEFT, primary right), mounts itself via `createRoot` into a detached container and defers unmount by a tick; async `onOk` drives a loading state and a rejection keeps the dialog open. Adopted at ALL NINE call sites (`Modal.confirm` no longer appears anywhere in src): ViewTabMenu, AttributeManagementPanel, ParameterRowsEditor, finance accounts/currencies/exchange-rates/balance-sheets, inventory scenarios-detail + AcquisitionForm; `okType:'danger'` and `okButtonProps:{danger:true}` both map to the `danger` flag (FR-031/SC-014). GOTCHA: the RTL suites all passed unchanged (they query by role/text), but FOUR **e2e** specs reached for `.ant-modal-confirm*`, which the shared dialog does not render — `inventory-scenario`, `inventory-acquisition`, `inventory-geometry` and `take-screenshots-011` were repointed at `[data-testid="confirm-dialog-footer"]` / `.ant-modal-content`. (3) `common.entityViews.close`: "Close tab" → "Close" (zh-TW 「關閉」). e2e `entity-views.spec.ts` grew to 11 tests (refresh-discards-scratch-tabs, URL-keeps-an-unpinned-view, revealed-survives-reload) — NOT run here (the live stack serves real data). Migrations 0006 and 0007 remain pending on the running docker stack. **Round 6 SHIPPED (2026-08-04)** — ONE defect, REPRODUCED before planning (frontend 755 vitest green + the 2 known full-suite-load flakes in balance-sheets/SyncTab which pass in isolation; backend 492 unchanged; research R40–R41): the unsaved-changes dot appeared on freshly loaded untouched views at seemingly random times. Root cause is a write-then-replay LOOP, not a display bug — `table.loadConfig()` lands in a LATER render, but the outbound URL effect re-runs in the SAME commit (its deps include `savedViews`, which just resolved), so it read the PRE-adoption snapshot and serialized the difference as override params (`?tbl.view=<id>&tbl.sort=&tbl.size=25` — "overrides" that are really the page defaults). That visit looked fine (the dot settles clean once adoption lands) but the URL was poisoned, and the NEXT load replayed those overrides on top of the stored config, so the view genuinely differed from its baseline → dot on arrival, then it rewrote itself. FIX: new `loadIntoTable(config, opts?)` helper is now the ONLY way this hook talks to `table.loadConfig` — it records the request in `pendingLoadRef`, and the outbound writer returns early until `reconcileConfig(table.snapshotConfig())` equals that pending value, then clears it. A comparison, NOT a boolean flag: `loadConfig` has no completion signal and a load requesting what the table already holds produces no state change, so a flag would never clear and the URL would go permanently silent (fail-open). The hook now carries FOUR navigation guards, each documented in a block comment with the failure it prevents (`lastParamRef` echo-skip, `lastProcessedRef` round-1 ping-pong, `inboundSettledRef` deep-link precedence, `pendingLoadRef` this round) — do not delete any as redundant. Dirty semantics UNCHANGED (FR-013 "differs from what's stored"). Tests assert the EMITTED PARAMS, not just the dot — a dot-only assertion passes while the URL is being poisoned for the next visit, which is how this survived rounds 2–5 (SC-015, 4 new hook tests + e2e double-reload, 12 tests in `entity-views.spec.ts`, NOT run here). KNOWN RESIDUE: a URL already bookmarked with real override params still loads dirty — FR-005 requires honouring overrides and a stale machine-written one is indistinguishable from a hand-edited one; navigating afresh is clean and no NEW poisoned URLs can be produced. Migrations 0006 and 0007 remain pending on the running docker stack. **Round 7 SHIPPED (2026-08-04)** — ONE defect, REPRODUCED from the user's steps (research R42): catalog → "Add empty view" → reload → the DEFAULT view arrived with the unsaved dot. A scratch tab has no stored view, so the outbound writer serializes it INLINE (`?inventory-catalog.f=and()`); on reload round 5 correctly discards the scratch tab, and the inbound inline branch then fell back to `DEFAULT_TAB_ID` and poured the blank config INTO the default tab — blanking the catalog's seeded YTD filter, so the table listed EVERYTHING while still labelled "YTD". A DATA-CORRECTNESS defect whose only visible symptom was the dot. FIX: inline state now creates its OWN unsaved tab (labelled "New view") and activates it; it may only reuse an existing tab when that tab is itself `kind:'anonymous'`; a tab representing a stored view (saved OR the default holder) is NEVER a valid target (FR-018). Creation happens inside the `setTabs` updater with an existence check + updater-form `setActiveTabId` (round-5 duplicate-tab discipline) because the inbound effect can re-run. The old fallback was fine while sessionStorage restored the scratch tab it belonged to — round 5's per-visit tabs made it fire on EVERY reload. The regression asserts the default tab's CONFIG, not just the dot (R41's lesson). One round-2 test that encoded the old behaviour ("inline params mark the DEFAULT tab dirty") now expects its own anonymous tab. **Round 8 SHIPPED (2026-08-04)** — GUARD RAILS, no behaviour change (frontend 764 vitest green, backend 492 unchanged; research R43). VERIFIED FIRST with a throwaway probe that the compactness directives ALREADY hold: saved view clean → `tbl.view=<id>`; edit → `+tbl.sort=name`; Save → back to `tbl.view=<id>` immediately (rounds 6–7 removed the mechanisms that violated them). The invariant suite passed on its first run — NO divergence was found and NO production code changed this round, which is the honest outcome rather than a manufactured fix. What landed: **FR-033** the bidirectional invariant — for the ACTIVE tab, indicator ⟺ ≥1 override param (a dot with no overrides = an invented difference, the round-6/7 defects; overrides with no dot = the URL describing state the table is not in, which poisons the next load); **FR-013** restated as exactly two cases (no stored view, or a stored view whose config differs) with INACTIVE tabs keeping their own dots because the URL only ever describes the active tab; **FR-034** the compactness rule written down. `expectIndicatorMatchesUrl(active, params, step)` (scoped to stored-view tabs; anonymous tabs serialize inline by definition) is asserted after load, edit, save, switch-away, switch-back and remount, plus a hand-edited deep link (SC-016) — AND a self-test proves the helper throws on both violation shapes, because a net that cannot fail proves nothing (the iteration-46 verify-the-verifier lesson). 6 new hook tests + 1 e2e (14 in `entity-views.spec.ts`, NOT run here — the live stack serves real data). Migrations 0006 and 0007 remain pending on the running docker stack. **Round 9 SHIPPED (2026-08-04)** — one FEATURE plus a NEGATIVE finding (frontend 771 vitest green, backend 492 unchanged; research R44–R45). **The two reported bugs are NOT in this branch.** The app the user tests at :3001 is a docker container (`unihub-frontend-1`) serving an image built 2026-08-11 20:24 — roughly two hours BEFORE the round-6 fix (a0309e8) and round-7/8 (bf0b2eb, 22:14). Both symptoms (unsaved dot after navigating to the catalog; inline/override URL after refresh) are exactly the round-6 write-then-replay loop and the round-7 inline-state-hijacks-the-default-tab defect, both already fixed here. EVIDENCE TRAIL, worth reusing: three vitest harnesses failed to reproduce (seeded filter → async attr columns → materialized default with an older column set), so a READ-ONLY Playwright probe drove the running app (login + navigation + GET only; `executablePath` pointed at an installed chromium because the pinned build was absent). It showed one tab "All - 2" (`zn6iFx8QhBMj`, is_default, stored `filters: []`, `pageSize: 100`) active with a dot while the table held page defaults. Instrumenting the adopt effect then produced ZERO console output in the container — which is what exposed the stale image; `docker ps`/`docker image inspect` confirmed it. LESSON (memory-worthy): before diagnosing a UI bug against localhost, verify WHICH artefact is being served — a container serving a built image silently ignores source edits and makes every observation describe old code. A T002 regression modelled on the real stored view was kept as a guard (it PASSES — the honest outcome, like round 8's T011); the two latent smells in the adopt effect (`defaultAdoptedRef` consumed before its guards; the "URL wins" guard reading live params) are RECORDED in R44 but NOT changed, because no failing test could be written against current source. **FEATURE — "Reset changes" (FR-035)**: a tab-menu item between Save and Close, enabled iff the tab is dirty, no confirmation. `resetTab(tabId)` restores a stored view's saved config or, for a tab with no stored view, the config it was CREATED with — which required a new in-memory `InternalTab.baseline` set by `addBlankTab` (blank), `duplicateTab` (the source's config) and the round-7 inline restoration (the URL's config); never persisted (round 5). Only the ACTIVE tab reloads the table, so resetting an inactive tab leaves the table and URL untouched; routing through `loadIntoTable` lets the round-6 pending-load gate drop the override params as the baseline lands (SC-018). Locale key `resetChanges` in both files. e2e grew to 16 tests (arrival stays clean across reload; reset clears dot + overrides) — NOT run here (the live stack serves real data). Migrations 0006/0007 remain pending on the running docker stack, which ALSO needs a frontend rebuild before any of rounds 6–9 is observable. **Round 10 SHIPPED (2026-08-12)** — the round-9 defect, REOPENED and actually fixed (frontend 774 vitest green / 4 skipped, backend 492 unchanged; research R46). **Round 9's "the bug is not in this branch" conclusion was WRONG and is retracted.** It rested on the :3001 container predating the fixes; the stack was rebuilt in the meantime (image 2026-08-11 23:53, twelve minutes NEWER than round 9's commit 37f4687), so the user's re-report described CURRENT code. Lesson recorded in R46 and memory [[env-localhost-3001-is-docker]]: an artefact-freshness check is valid only at the moment it is made — re-check before reusing its conclusion, and treat a repeated bug report as evidence about the code. REPRODUCED by driving the real page read-only with `history.pushState`/`replaceState` wrapped in an init script (every URL write recorded, not just the final address): arriving at the catalog produced `?inventory-catalog.view=zn6iFx8QhBMj&.f=<the page's YTD filter>&.sort=<the page's sort>&.size=50` — three "overrides" that are the PAGE defaults, against a stored default view holding `filters: []`, `pageSize: 100`. THREE stacked defects, each of which alone kept the flow broken: (1) **the pristine test read a stale snapshot** — adoption asked whether the default TAB's mount-time config still equalled the page defaults, but on a page whose columns arrive asynchronously (`attr:*`) `defaultConfig` GROWS and `reconcileConfig` appends the new keys at the END while the page declares them mid-order (`actions` is order 99), so the two can never be equal again and adoption bailed forever; it now compares the TABLE's live snapshot, which is the thing that knows whether the table is pristine. (2) **the one-shot burned before its guards and "the URL wins" read live params** — the two smells R44 recorded but declined to fix for want of a failing test (they were reachable all along); `defaultAdoptedRef` is replaced by `adoptedTokenRef` holding the config last offered (idempotent, re-entrant while the column universe settles, loop-proof since adoption changes `tabs`), and the URL question is answered from the mount-captured `initialUrlHadViewStateRef`, moved ABOVE its new consumer. `table.snapshotConfig` joins the adoption deps: the table's own column state is patched one commit AFTER `defaultConfig` gains the late columns, and a run inside that skew sees a legitimately different live snapshot. (3) **FR-037, the corrective write never fired** — the OUTBOUND effect compares `desired` against `currentJson`, but `searchParams` was not among its deps, so it ran on a closure captured before the write, read `current: []` while the address bar held four params, concluded the URL was already correct and skipped the write that must follow adoption; adding the dep is safe because the write is idempotent (the re-run finds `desired === current`). VERIFIED against the user's real data: nav-click and reload both end at a bare `/inventory/catalog`, no params, no dot. KNOWN RESIDUE: inside the skew window one override write still escapes and is corrected within the same second — closing it needs a gate that can tell "adoption is coming" from "the user edited during load", and every formulation tried either failed to fire (the skew IS a real difference) or risked silencing the URL permanently (the worse failure, R40); the root alternative — `reconcileConfig` merging unknown columns at their declared position instead of appending — changes column-drift for every saved view and belongs in its own round. Testing lesson: the round-9 harness holds a FIXED column set, which is exactly why it stayed green; the round-10 regression models late-arriving columns plus a saved-view list resolving after them, and fails on the old code with `expected 50 to be 25`. NOTE the round-9 e2e already asserted this flow and would have caught it — it was written but never run. New spec: FR-037 (publish decisions are made against the URL as it stands), SC-019 (async-column pages satisfy SC-017 identically). e2e grew to 17 tests (the new one pins that the STORED config is what's applied, which "no overrides, no dot" alone does not). **Round 11 SHIPPED (2026-08-12)** — two directives, and the first shares a root cause with the residue round 10 left open, so that closes too (frontend 785 vitest green / 4 skipped, backend 492 unchanged; research R47). REPORTED: `/inventory/catalog?inventory-catalog.view=<unpinned view>` showed the unsaved dot on the DEFAULT tab — one the user was not even looking at. TWO causes: (1) **adoption bailed entirely when the URL addressed a view**, so the default tab kept the PAGE config while being compared against its stored view; adoption is now TWO decisions — sync the default TAB's config (always, on screen or not) and load it into the TABLE (only when that tab is active and no URL view state claimed it). (2) **the "is this tab untouched?" test could never succeed** — `reconcileConfig` appended columns a config did not mention at the TAIL, so a snapshot captured before the catalog's `attr:*` columns loaded ordered them after `actions` while the page declares `actions` last, and nothing could bring the two back into agreement. FIX = the root alternative R46 named and deferred: **`mergeMissingByDeclaredOrder(listedKeys, declaredKeys)`** (new `components/EntityToolbar/columnOrder.ts`, 6 unit tests) places an unmentioned column immediately after its nearest DECLARED predecessor that the config does mention; adopted by BOTH `reconcileConfig` AND `useColumnConfig.loadState` — they must agree or the mismatch just moves from one comparison to another. This ALSO closes round 10's residue: with no skew there is no transient override write, and a nav-click to the catalog now emits NO url write at all (verified). BEHAVIOUR CHANGE, stated: a newly created parameter column now appears among the other parameter columns in an existing saved view instead of after "Actions" — that is what makes the comparison stable. **The catalog's own default view is GONE** (clarified — user asked): the seeded YTD filter, obtained-descending sort, 50/page and the `defaultViewName: 'YTD'` are removed (they shipped in iterations 17→24 and competed with the stored default view every account now has); the page contributes only its COLUMNS, and `DEFAULT_PAGE_SIZE` is exported from `useEntityTable` + the barrel so a page's `ViewConfig` baseline cannot drift from the table's own default into a false indicator. Consequence, accepted: an account with NO stored default view sees an unfiltered, unsorted catalog at 25/page — four catalog tests updated to the new contract (YTD tab name → generic "Table", Filter/Sort unlit, 25/page). **FR-038 no flash**: `ready` is set from INSIDE the pinned-merge effect (flag and tabs land in the SAME commit) and `ViewTabs` renders a height-reserving placeholder ahead of the collapsed branch; clarified as TAB ROW ONLY — the table keeps loading immediately. Measured on the real page by sampling the tab count every 50ms: `0 → placeholder → 2`, one transition. New spec: FR-038, SC-020, FR-021 amended (declared position, and every comparison must place such columns identically). e2e grew to 19 tests. **Round 12 SHIPPED (2026-08-12)** — "apply this view pattern to all entity views; the catalog is the most up-to-date" (frontend 793 vitest green / 4 skipped + the 3 known full-suite-load flakes in SyncTab/balance-sheets which pass in isolation, backend 492 unchanged; research R48). AUDIT FIRST, and it changed the round: **none of the other four pages seeded a filter, sort, page size or default-view name** — they were already closer to the round-11 pattern than the catalog had been, so editing them into a shape they already had would have been busywork. The REAL hazard they shared: each hand-copied the same `defaultViewConfig` object literal INCLUDING `pageSize: 25`, restating `useEntityTable`'s own default — the round-11 defect one edit away from returning (any drift between the page baseline and what the table actually opens at is reported as unsaved changes on a view nobody touched; five copies, five chances). FIX: **`viewConfigFromColumns(columnDefs)`** (new `components/EntityToolbar/viewConfig.ts`, exported from the barrel) builds the baseline from the page's columns plus the shared `DEFAULT_PAGE_SIZE`; ALL FIVE pages call it (`finance-accounts`/`finance-currencies`/`finance-exchange-rates`/`inventory-catalog`/`inventory-scenarios`). Its test asserts the property that matters — the page size it produces equals what a freshly mounted `useEntityTable` starts at — so the two cannot drift without a red test. **PER-PAGE LOCKS**: rounds 6–11 were only ever verified on the catalog (the only page with async columns); the other four inherited the fixes silently and now each assert (a) the first request carries no filter, no ordering and the default page size, (b) a stored default view is APPLIED on arrival with NO indicator — asserted in BOTH row shapes (`Unsaved changes` aria-label when expanded, `.ant-badge-dot` when collapsed) since which shows depends on the reveal state. VERIFY-THE-VERIFIER (the round-9 lesson): adoption was temporarily disabled and the new tests DO fail (`expected 25 to be 100`), then restored. TEST-ISOLATION GOTCHA: an appended sibling `describe` inherits NOTHING from the first block's `beforeEach`, and `revealed` persists in sessionStorage from earlier tests in the same file — the new blocks clear storage themselves. Read-only smoke probe against real data: all five pages render (7/25/25/138/4 rows), zero indicators, zero URL params, no page errors. New spec: FR-039 (one pattern, no per-page variation, ONE definition of the baseline), SC-021. **Round 13 SHIPPED (2026-08-12)** — two directives, the first a CORRECTION the user had raised before (frontend 787 vitest green / 4 skipped + the known SyncTab full-suite-load flake which passes in isolation, backend 492 unchanged; research R49). (1) **The FR-025 auto-hide is WITHDRAWN — removed, not disabled.** The row used to hide whenever a table had only its default view, behind a `TableOutlined` affordance carrying the dirty dot plus a per-table "revealed" preference. Gone: `collapsed`/`reveal` off `UseEntityViewsReturn`, the collapsed branch + Badge/Tooltip/Button out of `ViewTabs` (with the `collapsedRow` style and the `views.collapsed` effect deps), `common.entityViews.showViews` out of BOTH locales, the reveal step out of five page suites, and `revealRow` + the auto-hide test out of the e2e spec. FR-025 is REPLACED (the row is always shown); FR-009 no longer defers to it; the round-2 clarification is annotated superseded rather than rewritten. **That removal took the LAST persisted field with it**: round 5 had already reduced `useViewTabsState`'s payload to `{revealed}`, so the hook now writes NOTHING — its storage key, tolerant-parse helper, write effect and `tableKey` parameter are DELETED rather than left inert (a hook that persists nothing must not look like one that does), and its suite asserts `sessionStorage.length === 0`. (2) **"All entity types get default view named Table" is a CODE POLICY, not a label** — after round 11 no page passed `defaultViewName` and the localized fallback was already "Table"/「表格」, so what remained was the OPTION: any page could reintroduce a per-page name, which is exactly how the catalog's "YTD" came to compete with the account's stored default view (R47). `defaultViewName` is DELETED from `UseEntityViewsOptions`, so "Table" is the only initial name an entity table can have; FR-003 amended. CLARIFIED + IMPORTANT: **existing stored views KEEP their names** — the alternatives offered (auto-creating a "Table" row per table on first visit, or renaming stored defaults) would both have WRITTEN to the user's data to satisfy a naming policy, and neither is needed for the policy to hold going forward; the user's catalog still shows their own "All"/"All - 2". Four hook tests that encoded the withdrawn naming (virtual default named "YTD", materialized under it) were UPDATED not deleted — the virtual default now carries an EMPTY name and materializes as "Table" — because the materialization path they cover is still live. Read-only probe, all five pages: `row=1 collapsed=0 reveal=0 storage=0`, four tabs read "Table", catalog reads "All | All - 2". See [spec.md](specs/016-entity-views/spec.md), [research.md](specs/016-entity-views/research.md), [data-model.md](specs/016-entity-views/data-model.md), contracts/. **Deploy note (2026-08-11):** the local docker stack was rebuilt from this worktree (including the uncommitted round-7 frontend changes) and restarted — migrations core/0006 and 0007 are now APPLIED (verified via showmigrations), superseding the "pending on the running docker stack" notes in rounds 2–6. Docker builds on this machine egress through a flaky Windows fake-IP proxy; when they time out mid-download, see memory [[env-wsl-proxy-docker-builds]].

### Previous Feature (shipped): 018-inventory-enhancements

**Branch**: `018-inventory-enhancements` | **Plan**: [specs/018-inventory-enhancements/plan.md](specs/018-inventory-enhancements/plan.md)

Inventory app enhancements (GitHub issue #39), three independent changes — **SHIPPED 2026-07-22**: (1) **accumulated-cost ownership** — `CostFactor.user_managed` boolean (migration 0020): any manual edit to an accumulated line (incl. clear→0) freezes it as user-managed — stored verbatim on create (server derives ONLY when the payload carries no accumulated factor; the create-time "system-managed" rejection is GONE and `_reject_duplicate_accumulated` now guards create too), never auto-recalculated on any later edit; the form's reconcile effect re-derives AUTO rows live (fixing their stale-value latency — pre-018 an auto row kept its old value when the same currency's price changed), never touches user-managed rows, keeps user-managed rows whose currency lost its priced items (auto rows still drop; `''` placeholder only when NO accumulated row remains), and per-line Reset restores the derived Σ + auto state; one shared `factorPayload()` (accumulated verbatim + `user_managed`, manual `type || 'other'`) replaced manualPayload/fullPayload. Tests: pytest `TestAccumulatedOwnership` 8 (verbatim-zero create, any-accumulated-disables-derivation, dup-currency 400 on create, data_io auto-pickup) + RTL `AcquisitionForm.accumulated.test.tsx` 7 (payload locks, freeze/track, Reset re-arms, vanished-currency retention; NOTE: AntD keeps closed modals mounted — wait on `.ant-modal-wrap` display:none, and the page submit is "Create" in create mode). (2) **length unit default cm** — `DEFAULT_FAMILY_UNIT`/`defaultUnitFor(family)` in the inventory service at ParameterRowsEditor's three default sites (dropdown order + canonical mm untouched). (3) **catalog default pins** — `acquisition_summary` seeded `pin: 'left'` beside `__caret`, column key → `inventory-catalog-v8`; `column-pin.spec.ts` M-04 now locks TWO contiguous left pins mid-scroll (10/10 real-browser). Generated types regenerated via spectacular→file + openapi-typescript (no live server needed). NOTE: the user's running docker stack (built from the 015 worktree) has NOT been rebuilt/migrated — apply migration 0020 on next deploy. See [plan.md](specs/018-inventory-enhancements/plan.md), [spec.md](specs/018-inventory-enhancements/spec.md), [research.md](specs/018-inventory-enhancements/research.md), [contracts/acquisitions-api-delta.md](specs/018-inventory-enhancements/contracts/acquisitions-api-delta.md).

### Previous Feature (shipped): 015-data-migration-refinement

**Branch**: `015-data-migration-refinement` | **Plan**: [specs/015-data-migration-refinement/plan.md](specs/015-data-migration-refinement/plan.md)

Data migration refinement from GitHub issue #35. P1 bug: the sync push preview reported 1000+ phantom `inventory.item` deletions (shape of the catalog default filter's complement) — fix makes the server clone a disposable cache (fetch + hard-reset to remote head before every sync op), diffs the complete unfiltered dataset, and pins confirm to preview via `base_commit` + `diff_digest` (reproduction-first TDD on the `bare_repo` fixture). Also: change-preview tables get the constitution footer (size selector left of pagination), a commit-history graph in the Sync tab (`GET /api/v1/sync/history/`, custom commit-rail component, force-push detection via stored `last_known_remote_commit`), row-level staging on all previews (`excluded[]` + digest; partial publish = base rows + staged ops; selective topo-ordered apply with registry-FK dependency auto-include), and commit-node interactions (checkout preview/confirm of any compatible commit, header-validation compatibility gating) replacing the four legacy action buttons. Legacy `apply/preview` + `apply/confirm` endpoints removed. **All five stories SHIPPED (2026-07-20)** — US1: reset-first previews + `PreviewStaleException` pinning (regression trio incl. the stale-clone phantom-deletion reproduction); US2: preview tables adopt the shared `EntityOffsetFooter` via a client-paged `PagedPreviewTable` (no new footer component needed — it was already prop-driven); US3: `SyncHistoryView` + custom `CommitGraph` rail (blob-memoized `classify_commit` reuses the importer's extracted `validate_headers`; `last_known_remote_commit` ancestor check → rewritten banner); US4: hybrid-CSV partial publish (`excluded[]`, `NothingStagedException`) + `apply_selected` selective engine with transitive FK auto-include, tri-scope staging UI (NUL-separated `${table}\0${pk}` refs); US5: checkout preview/confirm on any compatible commit (partial checkouts do NOT move `local_state_commit`), `preview_from_commit` batch-FK validation, graph-node publish/checkout replacing the four legacy buttons ("Force publish" relabeled from "Force Push"). Backend 448 pytest / frontend 547 vitest green; note the sync service layer stays hand-typed per existing 004-era precedent (sync APIViews are not in the generated schema). **Refinement round SHIPPED 2026-07-21 (frontend-only — spec FR-021–FR-024 + FR-006/007/009 amendments, research R9–R12)**: constitution two-row `DateTimeCell` timestamps on commit nodes; per-node kebab menus (AntD Dropdown + MoreOutlined, no inline node buttons; disabled Checkout item carries the incompatible reason); content-fit tooltip targets; the uncommitted node auto-renders the staged publish review (`['sync','publish-preview']` query enabled by `has_local_changes` from the shared `useSyncHistory` hook, `pendingContent` slot on `CommitGraph`) replacing "Review & publish" + placeholder text; both Local/Remote-latest badges blue; history window client-passed `limit=10` initial / 20 per load-more. Frontend 565 vitest / backend 451 pytest green; vitest `testTimeout` raised to 15 s (interaction-heavy suites tripped the 5 s default under full-suite load — pre-existing flake). Manual six-point walk-through (quickstart.md §refinement round, T044) remains for a human session. **Round 2 — commit-rail polish SHIPPED 2026-07-22 (frontend-only, `CommitGraph.tsx` + locales; spec FR-025–FR-027 + FR-006/009/022 amendments, research R13–R15)**: bare rail (no "History" Card; `pages.io.sync.graph.title` key removed both locales), load-more as a terminal timeline node (`commit-node-load-more`; last commit `isLast` only when history exhausted), disabled kebab items label-only (reason stays on the node tooltip), sha7 hash as a default monospace Tag (uniform chip size with the blue badges), three-line node arrangement with single-line `YYYY-MM-DD HH:mm (relative)` timestamp — an explicit user-directed constitution deviation scoped to this surface (DateTimeCell no longer used here). Frontend 568 vitest / backend 451 pytest green; manual walk-throughs T044/T050 pending a human session. **Round 3 SHIPPED 2026-07-22**: publish confirm button relabeled "Publish Selected Changes" (`pages.io.sync.publishPreview.confirmButton`, zh-TW `發佈所選變更`; label-only, staging terminology unchanged). **Rounds 4–5 SHIPPED 2026-07-22**: (4) the grey app canvas now spans the full document height — ProLayout paints it only via the viewport-FIXED `.ant-pro-layout-bg-list`, so full-page captures showed a white band; fixed by painting `#f0f2f5` on the in-flow `.ant-pro-layout` in `src/index.css`, locked by the pixel-probe `e2e/layout-background.spec.ts` (research R16, FR-028/SC-009); (5) the checkout review renders embedded inside its target commit node via a `commitContent` slot on `CommitGraph`, symmetric with `pendingContent` (R17, FR-029/SC-010). Frontend 569 vitest green; PR #40 screenshots regenerated from the mock app. **Round 6 SHIPPED 2026-07-22**: the sider's right border now spans the full document height — the only boundary paint was the 1px border inside the viewport-fixed sider; fixed with `border-inline-start` on the in-flow `.ant-pro-layout-container` behind a `min-width: 768px` media query (ProLayout has NO dedicated mobile class, only `screen-*` tags — the naive `:not(.ant-pro-layout-mobile)` guard was inert and leaked the border into drawer mode at x=0; caught by a 375px probe). Second pixel-probe test in `e2e/layout-background.spec.ts` (R18, FR-030/SC-011). See [plan.md](specs/015-data-migration-refinement/plan.md), [research.md](specs/015-data-migration-refinement/research.md), and [contracts/sync-api.md](specs/015-data-migration-refinement/contracts/sync-api.md).

### Previous Feature (shipped): 017-multiple-sticky-columns

**Branch**: `017-multiple-sticky-columns` | **Plan**: [specs/017-multiple-sticky-columns/plan.md](specs/017-multiple-sticky-columns/plan.md)

Multiple sticky columns (GitHub issue #37): generalize column pinning from the two view-wide "pin first/last visible column" booleans to **per-column pin state** (`ColumnDef.pin?: 'left' | 'right'`) so any number of columns can be pinned to either table edge. **Iteration 48 (2026-07-20) shipped** — `useColumnConfig` rewritten: pin rides on each `ColumnDef` (defaults seed via page columnDefs, e.g. catalog `__caret` left / `actions` right, replacing the deleted `defaultSticky` option); ONE shared `(pinRank, order)` comparator (`compareDisplayOrder`) drives table AND panel order (rc-table needs fixed columns contiguous at the array edges; pins never mutate `order`, so unpinning returns a column home); new API `fixedForKey(key)` + `pinFingerprint` ("key:side|…" of visible pins, display order) replace `firstColumnFixed`/`lastColumnFixed` and join every PageTable remount key (constitution XII amended → v1.23.0). `ColumnPanel`: EVERY row carries pin-left + pin-right pushpins (`[data-column-row] [data-sticky-pin]`, right icon mirrored, i18n `pinLeft`/`pinRight` tooltips, aria-labels); click active side = unpin, other side = swap; apply-gated; hidden columns keep their pin (excluded from fingerprint/fixed until re-shown). All 6 consumer pages mechanically updated to `fixedForKey` + fingerprint keys. LATENT BUG FIXED: catalog `colDefMap` renderers close over `toggledIds` (itemFor/isExpanded) but never dep'd on it — masked before because the old hook rebuilt `visibleColumns` every render; the memoized hook exposed it (expand stopped splitting merged rows) — `toggledIds` added to the deps. Tests: hook suite 19 (grouping, fingerprint, seeds, patch preserves pin, referential stability), panel 24 (per-row buttons, mutual exclusion, TWO-left/TWO-right DOM classes with single fix-left-last/fix-right-first boundary), catalog RTL 34 (right-pin via panel, Reset restores default pins — needs 15s timeout: two panel round-trips + remounts in JSDOM), e2e `column-pin.spec.ts` 10/10 real-browser geometry at 600px viewport (two-left flush + contiguous ±1.5px mid-scroll, two-right hugging the right edge, both-sides with only the middle scrolling >100px, catalog defaults + Reset, hide/re-show retains pin, per-row-only pin buttons; NOTE: `.ant-table-body tr:first-child` is rc-table's hidden MEASURE row — geometry selectors must use `tr.ant-table-row`). No persistence (column settings stay per-visit; the hooks' `_key` params are unused — verified). See [plan.md](specs/017-multiple-sticky-columns/plan.md), [spec.md](specs/017-multiple-sticky-columns/spec.md), [research.md](specs/017-multiple-sticky-columns/research.md).

### Previous Feature (shipped): 014-inventory-app

**Branch**: `014-inventory-app` | **Plan**: [specs/014-inventory-app/plan.md](specs/014-inventory-app/plan.md)

Inventory domain from GitHub issue #33: an entity-centric Django app (`inventory`) + frontend section for cataloging **Items**, recording how each was obtained (**Acquisition**, with 1..N signed **CostFactor**s → per-currency `net_cost`), and per-situation planning via **Scenario** packing trees. Iterations 1–13 shipped (acquisition-first creation, cost factors, merged "Catalog" tree page with server-side filter/sort/pagination + flatten-on-item-filter, data_io registration, HTML legacy import, derived Item/Parameters/Acquisition columns + two-row datetime — see spec.md Clarifications for the full trail). Iterations 14 (dynamic parameters on core AttributeDefinition/AttributeValue + `attr:<id>` columns; constraints/checklist removed, Backlog+Organize detail) and 15 (merged single-item catalog rows, ×N tertiary, 4-case dates, zero-cost hidden, footer totals hook, legacy-import fixes + full 2026 re-import) shipped. Iteration 16 shipped (pinned Toggle ColumnDef + defaultSticky seed, ParameterRowsEditor form-grid + definition delete, scenario organize redesign: info panel, Add search modal, Splitter unorganized↔organized panes with HTML5 cross-pane drag, ScenarioItem.organized + move(organized)). Iteration 17 shipped (ICU-plural counts, plain Name column, URL width capped to render + gated tooltip, seeded default filter `obtained_at ≥ Jan-1 OR is_empty` via `useEntityFilter` defaultGroups, 50/page, 2025 append-import; fixes: is_empty-on-datetime 500, importer >4dp factor rounding). Iteration 18 shipped (Item.alias_name + shared ItemName, scenario list 2-col + detail Edit/kebab via ScenarioFormModal, unified dnd-kit organize: flattened tree + projectDrop projection, one-motion nested drops, real-mouse e2e). Iteration 19 shipped (constitution v1.21.0 PanelHeaderActions kebab pattern on scenario/acquisition panels, catalog Edit→href + Delete relocated to the edit page, organize caret restored + gated tooltips via ItemName.truncate, modal single-Add + acquisition context, full 2015–2024 import: 637 acq / 991 items verified). Iterations 20–25 shipped (20: constitution v1.22.0 ICU plurals, parser continuation fix, content-coverage sweep pytest over all real sheets, no-jitter tree drags; 21: flat-mode acquisition Edit link restored; 22: modal Add-button pixel-flush rows + geometric e2e lock; 23: date-cell no-data-loss — EOY default, latest-date complex cells, month-end `??` days, strikethrough skip, sweep covers date cells; 24: default catalog filter as ONE plain-OR condition group; 25: verbatim-first 備註 → item.spec/acquisition.remark, per-row prices → sku_price + summed accumulated override, STABLE import PKs via `legacy_ref` upsert — re-imports never clear scenarios). **Iteration 26 (2026-07-19) shipped** — temperature/time/battery unit families (affine °C↔°F via `core/units.py` family converters), dimension values accept `min-max` ranges (`AttributeValue.value_number_max`, `compute_value_fields` 4-tuple, range-validated editor input), parser 尺寸 triplet/pair split → 長/寬/高 parameters (verbatim kept when prose remains), shared `ItemDisplay` component (name/alias+URL primary, spec secondary, opt-in localized `key: value` parameter pairs) adopted at catalog Item cell, acquisition cards, scenario panes, and the Add modal; fixes: ItemFormModal initialize-on-open only (parent re-renders no longer wipe unsaved edits), organize-tree drops project from the POINTER (compact DragOverlay no longer misclassifies lower-half drops). **Iteration 27 (2026-07-19) shipped** — parameter emoji (`AttributeDefinition.emoji` + seeded 🎨👕⚖📏🧴 system defaults, monochrome `KeyEmoji` silhouette prefix in key-value pairs + key picker), FR-033 price format (`utils/currency.ts` symbol map + `formatPrice` → "TWD $ 129", zero/empty → "-"; shared `PriceInput`/`CurrencySymbolSelect` [symbol][amount] inputs at item modal + cost rows), catalog Actions pinned sticky-right (column key v7), Item column measured by primary name only (spec truncates at column width), Remark single-line + gated tooltip, side-nav real router links, equal-height item cards, Add-items modal wider (760) + viewport-anchored with inner-scrolling results; updated-sheet upsert re-import (658 acq / 1003 items, PKs + scenario memberships preserved). **Iteration 28 (2026-07-19) shipped** — ranges extended to plain `number`-typed parameters (same single-or-range grammar, min→`value_number`/max→`value_number_max`, validated text input replaces InputNumber), range display switched to tilde `min ~ max unit` everywhere, parser keyed patterns (長度/重量/容量) capture `min~max` ranges verbatim (`長度：74~164cm` → length 74~164 cm, canonical 740/1640 mm), upsert re-import repaired the ranged item (counts/PKs/scenario stable). **Iteration 29 (2026-07-19) shipped** — faithful organize drag preview: the DragOverlay renders the SAME row content (holder + ItemDisplay incl. spec/parameter pairs) at the grabbed row's measured width (captured in onDragStart), replacing the compact name-only chip; mid-drag e2e locks width ±2px + identical text (note: dnd-kit suppresses the first click after a drag — e2e spends it on a neutral spot). **Iteration 30 (2026-07-19) shipped** — keyed 寬度/高度/直徑/耐溫 extraction (signed ranges `-40~230`, 度C/℃→°C; NEW migration-seeded diameter 📏 + temperature 🌡 system definitions; importer measure keys + SYSTEM label maps + locales extended; upsert re-import: the 食品级折叠水杯 gains height 1.8~8cm / diameter 5.5~9cm / temperature −40~230°C), and the range input became an explicit **Exact|Range mode toggle** (`RangeValueInput`: mode select + one or two InputNumbers joined by ~, canonical text emitted, min≤max inline-validated) replacing the free-text syntax box on dimension AND number rows. **Iteration 31 (2026-07-19) shipped** — the drop indicator paints ABOVE the now semi-transparent drag preview (overlay zIndex 900 + opacity 0.75; indicator position:relative zIndex 1000, e2e-locked mid-drag), and the Add-items modal lists the 10 most recently acquired items while the search box is empty (`-acquisition__obtained_at__nullsfirst`, limit 10). **Iteration 32 (2026-07-19) shipped** — price currency selects display the full `{CODE} {symbol}` label when selected (bare symbol was ambiguous across $-sharing codes; `labelRender` removed from `CurrencySymbolSelect`, both consumers). **Iteration 33 (2026-07-19) shipped** — acquisition-edit item removals/edits are STAGED until Save (immediate deleteItem/updateItem removed — a real item was lost and restored via the ref-keyed upsert: 2026:3:1 MONDAY DUCK; 6-test RTL regression suite locks zero API calls before Save), and currency symbols now come from the FINANCE domain's Currency.symbol via a registry seeded in AppShell (hardcoded map deleted; TWD → NT$; unseeded codes render code-only). **Iteration 34 (2026-07-19) shipped** — the symbol registry became REACTIVE: `useCurrencySymbols()` (hooks/) subscribes to the shared finance currencies query, seeds the registry during render, and joins the catalog's width/column memo deps (the non-reactive module variable left warm-cache loads code-only forever); adopted at CatalogPage/AcquisitionForm/ScenarioDetail/AppShell; `utils/finance.ts` `getCurrencySymbol` now reads the same registry (second hardcoded map deleted); race locked by RTL with currencies resolving after list data. **Iteration 35 (2026-07-19) shipped** — key-value-only price extraction (plain 原價/單價 requires the colon; colonless only for the `N * M 件` quantity expression — 雨傘王 sku now derives from paid 725, prose 原價850 stays verbatim) and adorned paid cells parse via `extract_amount` (`¥4,200` → 4200 JPY; 東京迪士尼 rows recovered their paid amounts); updated sheets re-imported (658/1003, PKs + scenarios stable). **Iteration 36 (2026-07-19) shipped** — ItemDisplay remark comment-icon tooltip (flex primary line) + opt-in ⚠ deprecated warning (scenario panes + Add modal); stored `Item.deprecated` boolean (backfilled migration 0018, status derives from it, deprecate_time = when-known; catalog modal gains a "Deprecate time unknown" checkbox, Restore clears both, legacy deprecate_time-only payloads still derive the flag); parser per-unit dims (`50cm * 75cm` → 長/寬, mixed-unit triplets like `172cm x 58 cm x 4 mm`, tightened size-residue rule keeps `尺寸：S (…)`); re-imported (45 items with L+W pairs). **Iteration 37 (2026-07-19) shipped** — the remark comment icon (and ⚠) is suffixed directly to the item name: the primary-line name wrapper shrinks-to-fit (`flex: 0 1 auto`) so icons hug the text end instead of the row edge (RTL style-locked). **Iteration 38 (2026-07-19) shipped** — the remark/warning icons vertically CENTER on the name row (`alignItems: 'center'` — flex `baseline` floats SVG anticons off the text line; RTL style-locked). **Iteration 39 (2026-07-19) shipped** — 原價 (pre-discount list price) never sets the sku: hierarchy = explicit 單價 → own paid ÷ qty (原價X*N件 contributes qty only) → computed `原價X，N折` discount for shared-rowspan rows (失落文明 252 / 霍金 450, sum = the shared 702); derived skus inherit the acquisition currency. Re-imported: HEATTECH 380 TWD, Giordano 425 TWD, 內褲*2 159 TWD, 無印 fixture → 189. **Iteration 40 (2026-07-19) shipped** — segmented 備註 key-value parsing (FR-029j): lines split on ，/、/spaced-`/` (bare slashes stay in values), keys match per segment via the extracted `_apply_unit`, unconsumed segments → remark; delimiter-spanning forms (原價X，N折 / variants / whole-line 運費) keep whole-line processing; English `color:` joins the color keys. Re-imported: `size: L / 顏色: 00 WHITE` → size L + color 00 WHITE; `size: L，白色` → size L (白色 verbatim in spec). **Iteration 41 (2026-07-19) shipped** — KeyEmoji centers on the key text line (inline-flex + vertical-align middle + line-height 1; the emoji-key gap became a margin, so tag innerText has no literal space — e2e regexes must use ` ?`). **Iteration 42 (2026-07-19) shipped** — pattern-level parser rules (FR-029k): dims parts may be ranges (尺寸：Q（160x200x18~28cm） → 長160/寬200/高18~28cm + size Q); size `LABEL（annotation）` recurses the annotation through _apply_unit and keeps only the label; 腰圍/waist seeded (migration 0019, colon-optional key — size L(腰圍 84~92cm) → waist 84~92cm); acquisition-level `名稱原價…` listings assign skus by name-fragment with constraint-propagation matching (衣夾 frees 衣架) and progressive-prefix fallback (牛仔褲→褲), overriding shared-total header leaks; imports are per-year atomic with ref/name context on invalid values. All MUJI rows verified (被套1390/抹布119/衣架99×2; 衣架99×7/衣夾49; 衣架99×3/枕頭1390/褲1390 — TWD). Note: the editor key dropdown is virtualized past ~10 options — e2e must scroll `.rc-virtual-list-holder` to reach '+ New parameter…'. **Iteration 43 (2026-07-19) shipped** — the DragOverlay portals to document.body (transformed-ancestor jump remedy; grab-offset e2e ±3px), nest-drops color the prospective container row (strong tint + `data-nest-target`) and its subtree block (light tint) replacing the indicator line (sibling drops keep the line), and search marks reach every displayed text (ItemDisplay spec + modal acquisition context — note: HighlightText marks split direct text nodes, so RTL must assert via textContent/findAllByText). **Iteration 44 (2026-07-19) shipped** — shared-rowspan price repairs (FR-029l): a header paid cell spanning other ITEM rows never leaks into the header item's sku (killed entries still count toward the accumulated SUM; factor rows ignore carried totals — the bogus discount 3126 is gone); ANCHORED 備註 price segments (`原價 680` / `單價 449` / `原價 74 * 2 顆` / `原價：89 RM`, optional ¥￥$ prefix) form a list-price tier BELOW own-paid (單價 anchored → sku directly; prose stays protected — 雨傘王 intact); name-listing fragments accept paren annotations (大箱子(黑色) → the 黑色 50L box) with elimination for unmatched descriptors (AUKEY 充電器 2280); composite `color & size: 65 土黃L` splits on the trailing size token; qty units broadened to 件組個顆條包盒雙 (listing-derived qtys clear only when assigned to a sibling); importer aliases RM → MYR and CLEARS stale skus on upsert (partial-update gap). Re-imported all 12 years (658/1003, PKs + memberships stable); full old-vs-new diff reviewed: 100 item changes, all leak-removals/real-listed-prices/qty fixes. **Iteration 45 (2026-07-19) shipped** — pixel-verified alignment (FR-032/FR-011, repeatedly re-reported): parameter-emoji INK now centers on the tag row via runtime per-glyph canvas-metric compensation (`emojiInkShift`: TextMetrics ink box vs font box → translateY; box centering alone cannot fix per-glyph/per-platform ink placement — reproduced with an ink-level probe, 👕 sat 1.75px low), and the organize caret/holder center on the FULL row box (`alignItems: 'center'`, marginTop nudges removed — tree, flat pane, AND drag overlay; measured −22.5px/−35.9px before). NEW e2e `inventory-geometry.spec.ts` (deviceScaleFactor 4) locks both: emoji ink Δ ≤ 1.5px via canvas ink analysis of element screenshots, caret/holder Δ ≤ 1.5px vs row center. Also: scenario-list Name is a real router `<Link>` (href; onClick-only anchors banned, FR-010), Add-modal results render parameter pairs (FR-011), and scenario pages set browser tab titles via the new `usePageTitle` hook (`<page> · Unihub`, restore-on-unmount; detail = scenario name, FR-035). Process rule (memory + spec): visual-geometry complaints REPRODUCE first with a pixel probe + screenshots, fix by flex layout, lock with real-browser geometry assertions — JSDOM style locks don't count. **Iteration 46 (2026-07-19) shipped** — emoji ink MASK rendering (FR-032): iteration 45's metric compensation FAILED on real platforms and its e2e was vacuous (font-less environment rendered tofu); with Noto Color Emoji installed the shipped build failed its own test. New architecture: the glyph draws into a canvas, `inkBounds` pixel-scans its ACTUAL ink, the crop paints as a centered currentColor `mask-image` — displayed pixels ARE the measured pixels (construction-guaranteed on any font); JSDOM falls back to silhouette text. Shared `ParameterTag` (flex row) replaces the DUPLICATED tag markup (ItemDisplay + catalog Parameters column — where the 'fixed' bug survived); key-picker option flex-centered. Tests: inkBounds unit suite, KeyEmoji mask/fallback/cache RTL (mocked canvas), geometry e2e per-glyph over catalog/panes/modal + ANTI-TOFU guard (distinct glyphs must paint distinct masks). Lesson (memory): verify the verifier — probe environments must render the real thing. **Iteration 47 (2026-07-19) shipped** — the catalog Deprecate modal renders a bordered ItemDisplay preview of the target item (name/alias + URL, spec, remark icon, ParameterTag pairs — FR-003c) above the confirm line so the user verifies the right item; date/unknown-time controls unchanged; RTL-locked (preview shows name/spec/parameter pair). See [plan.md](specs/014-inventory-app/plan.md) and [research.md](specs/014-inventory-app/research.md).
<!-- SPECKIT END -->

## Active Technologies
- **Frontend**: TypeScript 5.7, React 18.3, Ant Design 5.24, @ant-design/pro-components 2.8, TanStack React Query 5, React Router 7, Vite 6, Vitest
- **Backend**: Python 3.12, Django 5.x, Django REST Framework 3.x, PostgreSQL 16, drf-spectacular, django-q2, httpx, gunicorn, uv
